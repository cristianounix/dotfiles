[github]
    user  = cristianounix
    email = cristianounix@gmail.com

[assembla]
    name  = cristianounix
    email = cristianounix@gmail.com

[user]
    name  = cristianounix
    email = cristianounix@gmail.com

[core]
    pager = less -R
    editor = /usr/bin/vim
    excludesfile = ~/.gitignore
    #excludesfile = /Users/cristainounix/.gitignore_global

    #[difftool "sourcetree"]
    #cmd = opendiff \"$LOCAL\" \"$REMOTE\"
    #path =

[push]
    default = tracking
    #default = matching
    #default = simple

[color]
    branch = auto
    diff   = auto
    interactive = auto
    status = auto
    ui = true

[color "status"]
    added = yellow
    changed = cyan
    untracked = red

#Merge com commit para marcar no grafo
[branch "development"]
    mergeoptions = --no-ff
    rebase = true

[branch "master"]
    mergeoptions = --no-ff

[pretty]
    # tut: http://gitimmersion.com/lab_10.html
    # ref: http://linux.die.net/man/1/git-log
    # Result: <short-sha> <commit-message> (<pointer-names>) -- <commit-author-name>; <relative-time>
    nice = "%C(yellow)%h%C(reset) %C(white)%s%C(cyan)%d%C(reset) -- %an; %ar"


[alias]
    who = !echo \"== Commits by Author:\\n`git shortlog -s -n`\\n\"
    st = status
    co = checkout
    ci = commit
    # lg = log --graph --abbrev-commit --decorate --date=relative --format=format:'%C(yellow)%h%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset) %C(bold yellow)%d%C(reset) %C(green)%ar%C(reset)'
    # lg = log --graph --abbrev-commit --decorate --date=relative --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all
    #l = log --graph --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n'' %C(white)%s%C(reset) %C(bold white)— %an%C(reset)' --abbrev-commit
    ll = log --graph --pretty=format:'%C(yellow)%h%Creset%C(blue)%d%Creset %C(white bold)%s%Creset %C(white dim)(by %an %ar)%Creset' --all
